var React = require('react');
var api = require("../api/api.js");

module.exports = React.createClass({
    getInitialState: function() {
        return {
        }
    },
    componentDidMount() {},
    render: function () {
        return (
            <div className="ui row section-dashboard">
                Dashboard
            </div>
        )
    }
});
