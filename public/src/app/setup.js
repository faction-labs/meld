var Install = require("./install.js")

module.exports = React.createClass({
    render: function () {
        return (
            <div className="pusher">
                <Install />
            </div>
        )
    }
});
